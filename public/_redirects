# www → apex НЕ се прави тук: Pages match-ва само път, не хост.
# Пренасочването е Redirect Rule на ниво зона в Cloudflare.

# ВНИМАНИЕ при добавяне на нови файлове в /images и /videos.
#
# Заявка за несъществуващ файл пада в catch-all-а отдолу и получава
# index.html със статус 200. Правилото `/images/*` в `_headers` слага на този
# отговор седемдневен кеш, тоест адресът на снимката остава закеширан с HTML
# за цяла седмица. Достатъчно е да поискаш новия адрес секунди преди деплоят
# да е стигнал до edge-а.
#
# Затова: НЕ отваряй адреса на нова снимка, докато HTML-ът на живо не я сочи.
# Ако все пак се случи, лекарството е ново име на файла (виж `-v2` суфиксите).
#
# Опитах да върна истински 404 за тези пътища, но Pages поддържа в `_redirects`
# само 200 и пренасочванията. Страница на име 404.html пък не става: Pages я
# сервира за всеки непознат път и убива SPA рутинга.

/* /index.html 200
